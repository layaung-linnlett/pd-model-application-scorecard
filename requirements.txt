# PD Application Scorecard — Python dependencies (Python 3.11+)
# Install with:  pip install -r requirements.txt

# Data handling
pandas==3.0.3
numpy==2.4.6

# Visualisation
matplotlib==3.11.0
seaborn==0.13.2

# Machine learning
scikit-learn==1.9.0

# Model persistence
joblib==1.5.3

# --- Deferred: not installed until the logistic-regression baseline is approved ---
# xgboost==3.2.0          # pinned: 3.3.x needs Python 3.12+
# imbalanced-learn==0.14.2  # SMOTE — only if class_weight='balanced' proves insufficient
